handling ""

decide whether to consider height in geodesic calculations or not

implement brute force algorithm that will tell the shortest perimeter (obwód). Make permutations without duplicates. It would be a problem if there will be lots of points.